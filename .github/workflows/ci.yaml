name: CI

on: [push]


jobs:

  build:
    runs-on: ubuntu-latest
    steps: 
     - uses: actions/checkout@v4
     - run: |
        echo "Multiline"


  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "pipeline radi"

  # lint:

  # deploy:
